# Server Configuration
server.port=8080
server.servlet.context-path=/api

# MongoDB Configuration
# For local MongoDB
spring.data.mongodb.uri=mongodb://localhost:27017/travelsri
spring.data.mongodb.database=group_tour

# For MongoDB Atlas (Cloud) - uncomment when needed
# spring.data.mongodb.uri=mongodb+srv://hasithchamika2001:Atlas4321@cluster0.uj1pkoj.mongodb.net/travelsri?retryWrites=true&w=majority&appName=Cluster0

# JWT Configuration
jwt.secret=mySecretKey123456789012345678901234567890
jwt.expiration=86400000

# CORS Configuration
spring.web.cors.allowed-origins=http://localhost:8081,http://localhost:19006
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# Logging
logging.level.org.springframework.data.mongodb=DEBUG
logging.level.org.springframework.web=DEBUG

# Application Configuration
spring.application.name=travelsri-app